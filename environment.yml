name: upsnet_pytorch
channels:
  - defaults
  - anaconda
dependencies:
  - python=3.6
  - pytorch=1.0
  - pip
  - pip:
    - pyyaml
    - pycocotools
    - tensorboardX
    - freeze
    - image
    - opencv-python 
    - easydict
    - networkx
